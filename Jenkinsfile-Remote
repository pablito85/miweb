pipeline {

    agent any

    stages {

        stage(‘update’) {

            steps {

                sh "git add Jenkinsfile-Remote"
		sh 'git commit -m "another one"'
		sh "git push primary"

            }
            

        }

    }

}
